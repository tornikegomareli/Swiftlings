// ex02_types_inference.swift
// Basic Syntax: Type Inference

// In this exercise, you will explore how Swift infers types based on initializers.
// You will also practice overriding the inference with explicit type annotations.
// Follow the TODOs to complete the code.

// 1. Declare a constant 'inferredInteger' initialized to 42 (without specifying type).
//    Swift should infer it as 'Int'.
// 2. Declare a constant 'inferredDouble' initialized to 3.14 (without specifying type).
//    Swift should infer it as 'Double'.
// 3. Now declare 'explicitFloat' as type Float, also with an initial value of 3.14.
// 4. Print out the type of each variable using Swift's reflection or just mention it in a comment.
// 5. Attempt an assignment that violates type inference (e.g., assigning a string to your integer). Observe the error.

func typeInferenceDemo() {
    // TODO: Step 1 - Declare 'inferredInteger' with value 42

    // TODO: Step 2 - Declare 'inferredDouble' with value 3.14

    // TODO: Step 3 - Declare 'explicitFloat' with type Float = 3.14

    // TODO: Step 4 - Print out the variables (optionally, discuss their types in comments)

    // TODO: Step 5 - Attempt to assign a string "Hello" to 'inferredInteger' or 'inferredDouble'
    // and see the compiler error. Then remove or comment out that line.
}
